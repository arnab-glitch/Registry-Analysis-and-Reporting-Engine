# RegEx-Analysis — Requirements

This tool is lightweight and requires only Python and the Jinja2 template engine
to generate interactive HTML forensic reports.

---

## Software Requirements
- Python 3.8 or newer  
- Any modern web browser (Chrome, Edge, Firefox) to view the HTML output

---

## Python Libraries

### Built-in (No installation needed)
- pathlib
- csv
- json
- datetime
- os
- sys

### External (must be installed)
- jinja2

Install Jinja2 using:

```bash
pip install jinja2
